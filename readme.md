
ln -s ~/Dotfiles/nvim ~/.config/nvim

ln -s ~/Dotfiles/kitty ~/.config/kitty

ln -s ~/Dotfiles/.bashrc ~/

also make a .desktop file in .local/share/applications/ with 'Exec:kitty -e nvim %F'

misc: Brightness 79, Contrast 7
